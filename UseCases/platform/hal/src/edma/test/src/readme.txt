Test Scope:

This unit test covers most of edma hal interface and verify its operation.

NOTES:

Make sure project is rebuild before testing.

Test Setup:

1. BAUDRATE: 115200.
2. UART PORT:
   2.1 k64: UART(USB) port on CPU board.
   2.2 K22: UART port on Serial board.
   2.3 K70: UART port on Serial board.
   2.4 V11: UART port on Serial board.

Output:

EDMA hal driver unit test start!
EDMA hal driver unit test Finished successfully!

