Test Scope:

This unit test covers most of LPTMR hal interface and verify its operation.

NOTES:

Make sure project is rebuild before testing.

Test Setup:

1. BAUDRATE: 115200.
2. UART PORT:
   2.1 k22: UART port on CPU board.
   2.1 k64: UART port on CPU board.
   2.1 k70: UART port on CPU board.

Output:

Instance 0: LPTMR HAL all true test PASSED
Instance 0: LPTMR HAL all false test PASSED
